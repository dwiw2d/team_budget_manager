# CLOVA General 파서 인식률 기준선 (before / after)

측정일: 2026-09-18
대상: `supabase/functions/ocr/clova-general.ts` 의 `extract()`
채점기: `node scripts/ocr-bench/score.mjs` (파서는 이번에도 한 줄도 고치지 않았다)

## 판본 — 숫자를 읽기 전에 먼저 읽어라

**판본(항목 수 + sha256)이 다른 숫자끼리는 비교하지 마라.** 차이가 파서 탓인지 정답표 탓인지
갈리지 않는다. 이 문서의 모든 표에는 어느 판본으로 쟀는지가 붙어 있다.

| 이름 | 파일 | 항목 수 | sha256 (앞 16자) |
|---|---|---:|---|
| **동결본 v1** | `.local/receipt-corpus/frozen/catalog-v1.json` | 134 | `510fc7a873c7e45c` |
| **v1 앞 38** | 위 파일의 앞 38개 항목만 추린 부분집합 | 38 | `3158b5802bfa447a` |
| **판본 미기록** | 그때의 `catalog.json` (이미 덮여 사라짐) | 135 | 기록 안 함 |

`.local/receipt-corpus/catalog.json` 은 수집 작업이 도는 동안 계속 바뀐다. 실제로 첫 측정과
재측정 사이 23분 동안 채점 항목 111개 중 54개가 사라지고 15개는 같은 id 에 다른 사진이 붙었다.
그래서 **동결본을 만들고 채점기가 그것을 먼저 읽게 바꿨다.** 채점기는 이제 실행할 때마다
경로·항목 수·sha256·채점한 항목 수와 필드 수를 출력 맨 위에 찍는다.

"판본 미기록" 으로 표시된 표는 사라진 카탈로그에서 잰 값이라 **재현할 수 없다.** 참고로만 남긴다.

### 어느 커밋을 쟀는지

| 열 | 커밋 | 무엇인가 |
|---|---|---|
| **before** | `89e8886` | 상호 규칙 수정 **전**. 첫 기준선(68.7%)을 낸 파서. |
| **after** | `2c7cefd` | 상호 규칙 수정 **후**. 현재 `receipt-tracker-pwa` 본류. |

`after` 는 `84b7ab2`(상호 거름망이 진짜 상호를 버리던 문제)와 `6369bc1`('…스시'·'…장군'이
주소로 오인되던 문제) 두 수정을 포함한다.

## 도달 가능 / 도달 불가 — 분모가 두 벌인 이유

정답(`truth`)이 `visible_lines` 에 **글자로 아예 없는** 필드가 있다. 로고로만 찍혔거나, 그
크롭에 그 부분이 안 나왔거나, 정답이 두 줄에 갈라져 있는 경우다. 글자가 없으니 **어떤 파서
규칙으로도 못 맞힌다** — 2단계 Gemini 몫이다. 이걸 분모에 그냥 두면 "규칙만으로 100%" 라는
불가능한 목표를 세우게 된다. 그래서 채점기가 필드마다 둘로 나눈다.

- **도달 가능**: `truth` 가 `visible_lines` 안에 있다. 여기서의 100% 가 **규칙으로 닿는 최대치**다.
- **도달 불가**: `truth` 는 있는데 `visible_lines` 에 없다.

판정은 필드마다 표기가 달라 아래처럼 정규화한 뒤 **줄 단위로** 포함 여부를 본다
(`scripts/ocr-bench/lib/reachable.mjs`). 줄을 넘나드는 비교는 하지 않는다 — 정답이 두 줄에
걸치면 도달 불가 쪽으로 보수적으로 센다.

| 필드 | 어떻게 비교했나 |
|---|---|
| `merchant` | 공백만 지우고 비교. 채점의 `compare()` 와 같은 잣대다. |
| `amount` | 숫자만 남겨 비교. `21000` 과 `"합계 21,000원"`(→`21000`)이 같아진다. 자릿수가 칸으로 쪼개진 `"4 4 0 0"` 도 이어붙으면 잡히므로 도달 가능으로 센다. |
| `cardNumber` | 채점과 같은 `cardDigits()` 로 마스킹 문자(`x`·`×`·`●` 등)를 `*` 로 통일하고 숫자·`*` 만 남겨 비교. |
| `paidAt` | **날짜만** 본다(시각은 안 본다 — 채점이 날짜만 맞아도 부분 정답을 주기 때문). `2021-04-19`·`2021년 4월 19일`·`21.04.19`·`20210419` 를 모두 같게 보려고 연(네 자리/두 자리)·월·일의 0 없는 형태까지 허용하는 정규식으로 맞춘다. |

숫자를 이어붙이는 비교는 애매한 경우를 **도달 가능 쪽으로 넉넉히** 센다. 그러면 도달 가능
분모가 커져 정답률이 낮게 나오므로, 판정이 틀려도 "규칙으로 닿는 최대치"를 부풀리지 않는다.

## 재현 방법

```sh
# 채점기가 카탈로그를 찾는 순서:
#   1) RECEIPT_CATALOG 환경 변수의 파일
#   2) $RECEIPT_CORPUS_DIR/frozen/catalog-v1.json   (동결본 — 기본값)
#   3) $RECEIPT_CORPUS_DIR/catalog.json             (살아 있는 카탈로그)
# RECEIPT_CORPUS_DIR 기본값은 .local/receipt-corpus.
node scripts/ocr-bench/score.mjs
node scripts/ocr-bench/score.mjs --json   # 회귀 비교용. catalog 블록에 판본이 들어 있다.
RECEIPT_CORPUS_DIR=../wolfeel/.local/receipt-corpus node scripts/ocr-bench/score.mjs

# "v1 앞 38" 부분집합 만들기 — 첫 기준선과 같은 표본이다.
node -e "const fs=require('fs');const c=JSON.parse(fs.readFileSync('.local/receipt-corpus/frozen/catalog-v1.json','utf8'));fs.writeFileSync('/tmp/catalog-v1-first38.json',JSON.stringify(c.slice(0,38),null,2))"
RECEIPT_CATALOG=/tmp/catalog-v1-first38.json node scripts/ocr-bench/score.mjs

# 채점기 자체의 단위 테스트 — 글로브를 반드시 따옴표로 묶어라.
node --test "scripts/ocr-bench/lib/*.test.mjs"
```

> **따옴표 없는 디렉터리 인자는 쓰지 마라.** Node 24 는 `node --test scripts/ocr-bench/lib/`
> 처럼 디렉터리를 주면 그 디렉터리 자체를 테스트 모듈로 읽으려다 `'test failed'` 로 끝난다
> (v24.14.0 확인: `pass 0 / fail 1`). 글로브를 따옴표로 묶어 Node 가 직접 펼치게 해야 한다.

카탈로그가 없으면 찾은 순서를 적고 exit 2 로 끝난다. CLOVA·Gemini API 는 호출하지 않는다.

## 1. v1 앞 38 기준 — 판본 `3158b5802bfa447a`, 38장 중 33장 채점 / 67필드

첫 기준선과 같은 표본이다. **동결본으로 다시 돌려 46/67(68.7%)·오답 21건·조용한 오답 6건이
글자까지 재현됐다.** 그래서 이 표본에 한해서는 판본 미기록 카탈로그에서 잰 before 열과
비교해도 된다.

| 필드 | 도달 가능 정답/분모 | 도달 가능 정답률 | 전체 정답/분모 | 전체 정답률 | 도달 불가 |
|---|---:|---:|---:|---:|---:|
| merchant | 4/12 | 33.3% | 4/15 | 26.7% | 3 |
| paidAt | 21/23 | 91.3% | 21/23 | 91.3% | 0 |
| amount | 17/24 | 70.8% | 17/24 | 70.8% | 0 |
| cardNumber | 4/5 | 80.0% | 4/5 | 80.0% | 0 |
| **전체** | **46/64** | **71.9%** | **46/67** | **68.7%** | **3** |

**규칙으로 닿을 수 있는 최대치**(도달 가능 분모 ÷ 전체 분모):

| 필드 | 규칙 상한 | 못 닿는 항목 |
|---|---:|---|
| merchant | **80.0%** (12/15) | 026, 027, 030 |
| paidAt | 100% (23/23) | — |
| amount | 100% (24/24) | — |
| cardNumber | 100% (5/5) | — |
| **전체** | **95.5%** (64/67) | 위 3건 |

before/after 비교(before 열은 판본 미기록 카탈로그, after 열은 v1 앞 38):

| 필드 | before 정답/분모 | before 정답률 | after 정답/분모 | after 정답률 | 변화 |
|---|---:|---:|---:|---:|---:|
| merchant | 4/15 | 26.7% | 4/15 | 26.7% | ±0 |
| paidAt | 21/23 | 91.3% | 21/23 | 91.3% | ±0 |
| amount | 17/24 | 70.8% | 17/24 | 70.8% | ±0 |
| cardNumber | 4/5 | 80.0% | 4/5 | 80.0% | ±0 |
| **전체** | **46/67** | **68.7%** | **46/67** | **68.7%** | **±0** |

조용히 틀린 건수(`weak` 표시 없이 틀림):

| 필드 | before | after | 변화 |
|---|---:|---:|---:|
| merchant | 2 | 2 | ±0 |
| paidAt | 0 | 0 | ±0 |
| amount | 3 | 3 | ±0 |
| cardNumber | 1 | 1 | ±0 |
| **전체** | **6** | **6** | **±0** |

**이 표본에서는 상호 규칙 수정이 아무것도 바꾸지 않았다.** 정답률도, 조용한 오답 수도,
개별 항목의 출력값도 21건 전부 before 와 글자 하나까지 같다.

## 2. 동결본 v1 전체 — 판본 `510fc7a873c7e45c`, 134장 중 112장 채점 / 297필드

**지금 파서가 어디쯤인지는 이 표를 봐라.**

| 필드 | 도달 가능 정답/분모 | 도달 가능 정답률 | 전체 정답/분모 | 전체 정답률 | 도달 불가 |
|---|---:|---:|---:|---:|---:|
| merchant | 16/68 | 23.5% | 16/75 | 21.3% | 7 |
| paidAt | 84/93 | 90.3% | 84/95 | 88.4% | 2 |
| amount | 72/101 | 71.3% | 72/101 | 71.3% | 0 |
| cardNumber | 22/26 | 84.6% | 22/26 | 84.6% | 0 |
| **전체** | **194/288** | **67.4%** | **194/297** | **65.3%** | **9** |

**규칙으로 닿을 수 있는 최대치**:

| 필드 | 규칙 상한 | 못 닿는 항목 |
|---|---:|---|
| merchant | **90.7%** (68/75) | 026, 027, 030, 107, 127, 129, 191 |
| paidAt | **97.9%** (93/95) | 127, 174 |
| amount | 100% (101/101) | — |
| cardNumber | 100% (26/26) | — |
| **전체** | **97.0%** (288/297) | 위 9건 |

오답 성격:

| 필드 | 부분 정답 | 오답(weak 표시됨) | 오답(조용) | 오답 합계 |
|---|---:|---:|---:|---:|
| merchant | 0 | 52 | 7 | 59 |
| paidAt | 5 | 10 | 1 | 11 |
| amount | 0 | 8 | 21 | 29 |
| cardNumber | 0 | 0 | 4 | 4 |
| **전체** | **5** | **70** | **33** | **103** |

> before 열이 없는 이유: before(`89e8886`) 를 잰 카탈로그는 판본 미기록 135장이고 이미
> 사라졌다. 같은 판본으로 다시 잴 수 없으므로 여기서는 after 만 싣는다. 아래 §3 의
> before/after 차이도 같은 이유로 v1 에서 확인되지 않는다.

## 3. 상호 규칙 수정은 도움이 됐나 (판본 미기록 135장 — 재현 불가)

> **경고: 이 절의 before/after 는 사라진 판본에서 잰 값이다.** v1 로 다시 잴 수 없다.
> 아래에서 근거로 든 8개 항목 중 `308`·`309`·`310`·`311`·`9114` 는 v1 에 아예 없다.
> v1 에서 재현되는 것은 after 쪽 merchant 조용한 오답 **7건** 하나뿐이다.

판본 미기록 135장 기준으로는 **정답률이 오르지도 내리지도 않았다(정확히 ±0)**. 135장 전체에서
correct 로 바뀐 항목이 0건, wrong 으로 뒤집힌 항목도 0건이었다. 두 커밋의 제목이 약속한 것에
해당하는 표본이 그 카탈로그에 **채점 가능한 형태로는 하나도 없었다** — 회귀는 안 났지만 이득도
측정되지 않았다.

수정이 실제로 바꾼 것은 딱 하나, **조용한 오답 8건을 표시된 오답으로 돌린 것**이었다.
`findMerchant` 가 `{value, sure}` 를 돌려주게 바뀌면서, 라벨 없이 사업자번호 숫자 모양만 보고
윗줄을 집은 경우 `sure=false` 로 `weak` 에 들어가게 된 덕이다. **요약하면 이 수정은 인식률
개선이 아니라 안전장치 개선이다.** 틀린 답을 덜 틀리게 만들지는 못했고, 틀린 답이 사용자에게
조용히 흘러가는 경로를 막았다. 숫자를 꾸며서 보고할 것이 없다.

## 4. 오답 원인별 분류

### 4-1. v1 앞 38 기준 21건 — 판본 `3158b5802bfa447a`

| 원인 | 필드 | 건수 | 항목 | 규칙 |
|---|---|---:|---|---|
| 라벨·콜론이 없는 구조(제목형·표 칸형)라 세 휴리스틱 모두 실패 → null | merchant | 4 | 078, 079, 080, 087 | `findMerchant` 전체 |
| `findMerchant` (b) 가 '전화' 줄 오른쪽 끝에서 앱 버튼 이름을 집음 | merchant | 2 | 077, 091 (둘 다 `"가게보기"`) | `findMerchant` (b) |
| `merchantOk` 의 `\d{3}` 이 숫자 든 법인명을 오탈락 → null | merchant | 2 | 051, 052 | `merchantOk` |
| **도달 불가** — 정답이 `visible_lines` 에 글자로 없음 | merchant | 3 | 026, 027, 030 | 파서 문제 아님 |
| `AMOUNT_YES` 다수결이 부가세 제외 금액(공급가)을 고름 | amount | 2 | 002, 003 | `findAmount` 다수결 |
| 라벨이 `AMOUNT_YES` 에 없어 fallback 최댓값이 엉뚱한 숫자를 집음 | amount | 3 | 051, 052, 091 | `AMOUNT_YES` / fallback |
| `받은금액` 이 없고 `합계`(할인 전)가 먼저 잡힘 | amount | 1 | 081 | `AMOUNT_YES` |
| 금액이 자릿수 칸으로 쪼개져(`4 4 0 0`) fallback 이 연도(2020)를 집음 | amount | 1 | 078 | `lastNumber` / fallback |
| 한글 `YYYY년 MM월 DD일 오후 H:mm` 을 `parseDate` 가 못 읽음 | paidAt | 1 | 091 | `parseDate` |
| 날짜와 시각이 인접 줄이 아니라 시각을 못 찾음(부분 정답) | paidAt | 1 | 059 | `findPaidAt` 인접줄 탐색 |
| **정답표 오류** — 실제 영수증은 별표가 16자인데 정답표가 10자로 적혀 있음 | cardNumber | 1 | 060 | 파서 문제 아님 |

`030` 은 전에 "라벨 없는 구조" 로 묶여 있었으나 도달 불가로 옮겼다. 정답
`"파리바게뜨 여의도KBS점"` 이 `visible_lines` 에 `"여의도KBS점"` / `"파리바게트"` 두 줄로
갈라져 있고 표기(`뜨`/`트`)까지 달라, 어떤 규칙으로도 이 정답 문자열에 닿지 못한다.

### 4-2. 판본 미기록 135장 기준 107건 — 재현 불가, 재분류 필요

> **경고: 아래 묶음은 사라진 판본 135장(오답 107건)에서 낸 것이다.** 동결본 v1 의 오답은
> 103건이고 필드별 분포도 다르다(merchant 59 / paidAt 11 / amount 29 / cardNumber 4).
> **v1 기준 재분류는 아직 안 했다.** 원인의 종류를 보는 참고용으로만 읽고, 건수를 인용하지 마라.

| 필드 | 원인 | 건수 (판본 미기록) |
|---|---|---:|
| merchant | 로고체 상호가 라벨 없이 단독 줄 → null (POS·백화점·편의점 계열) | 22 |
| merchant | 엉뚱한 값을 집음(임대인 로고 `Starfield`, 영문 `LOTTERIA`, 라벨어 포함 `영화관 …`, 잘린 주소 `워 B동`, 앱 버튼 `가게보기` 등) | 13 |
| merchant | 법정서식 표 칸형(진료비·약제비 별지 서식)에 라벨이 없음 → null | 10 |
| merchant | 모바일 앱 캡처라 라벨 없이 값만 나열 → null (GS25) | 6 |
| merchant | VAN 안내문구 선행 / 가맹점-판매자 이중구조 → null | 4 |
| merchant | `merchantOk` 의 `\d{3}` 오탈락 → null | 2 |
| merchant | `MERCHANT_ROLE` 이 더미 상호를 함께 걸러냄 → null | 2 |
| amount | 라벨 없어 fallback 최댓값이 엉뚱한 숫자를 집음 | 9 |
| amount | 의료·약제비 서식에서 본인부담금 대신 '총액'을 고름 | 8 |
| amount | 할인 전 `합계`를 고름 | 3 |
| amount | 자릿수 오독(`11000`→`110000`) / 자릿수 칸 분리 | 3 |
| amount | 공급가(부가세 제외)를 고름 | 2 |
| paidAt | 시각을 못 찾아 `00:00:00` (부분 정답) | 6 |
| paidAt | 초 단위를 남김 / 12시간제 '오후' 미해석 / 엉뚱한 날짜 | 5 |
| paidAt | 날짜만 있는 게 정답인데 `T00:00:00+09:00` 를 붙임 | 3 |
| paidAt | 날짜 자체를 못 찾음 → null | 3 |
| paidAt | 한글 `YYYY년 MM월 DD일 오후 H:mm` 을 못 읽음 | 1 |
| cardNumber | 다른 번호를 집음 / 마스킹 자릿수 불일치 / 12자 미달 | 5 |

## 5. 99% 까지 무엇을 고쳐야 하는가

**고치지 않았다. 무엇을 고쳐야 하는지만 적는다.**

### 먼저 알아야 할 것: 규칙만으로는 100% 가 애초에 불가능하다

- **v1 앞 38 (67필드)**: 도달 불가 3건. 규칙 상한은 **64/67 = 95.5%**. 현재 46 정답.
- **동결본 v1 (297필드)**: 도달 불가 9건. 규칙 상한은 **288/297 = 97.0%**. 현재 194 정답.

99% 는 **두 표본 모두 규칙만으로는 넘을 수 없는 선**이다. 도달 불가 항목은 2단계 Gemini 보완에
맡기거나 표본에서 빼야 한다. 표본이 작아 한 필드가 1.5%p(앞 38) / 0.34%p(v1 전체)씩 움직인다.

### v1 앞 38 기준 개선 목록 — 판본 `3158b5802bfa447a`

1건 = 전체 기준 +1.5%p(1/67), 도달 가능 기준 +1.6%p(1/64).

| # | 고칠 것 | 건수 | 누적 정답률 (도달 가능, /64) | 누적 정답률 (전체, /67) |
|---:|---|---:|---:|---:|
| — | 현재 | — | 46 → 71.9% | 46 → 68.7% |
| 1 | 라벨 없는 제목형·표 칸형 상호를 자리로 찾기(첫 줄 후보 + 사업자번호 상대 위치) | 4 | 50 → 78.1% | 50 → 74.6% |
| 2 | `AMOUNT_YES` 에 없는 라벨(`청구액`·`받은금액`·`총주문금액`) 추가 + fallback 최댓값 폐기 | 4 | 54 → 84.4% | 54 → 80.6% |
| 3 | `findMerchant` (b) 오른쪽 끝 토막에서 앱 버튼·UI 문구 거르기(`가게보기` 등) | 2 | 56 → 87.5% | 56 → 83.6% |
| 4 | `merchantOk` 의 `\d{3}` 완화 — 숫자 든 법인명(`서울법인115`) 허용 | 2 | 58 → 90.6% | 58 → 86.6% |
| 5 | `findAmount` 다수결에서 공급가·부가세 줄을 후보에서 제외 | 2 | 60 → 93.8% | 60 → 89.6% |
| 6 | 자릿수 칸으로 쪼개진 금액(`4 4 0 0`) 병합 | 1 | 61 → 95.3% | 61 → 91.0% |
| 7 | `parseDate` 에 한글 `YYYY년 MM월 DD일 오전/오후 H:mm` 추가 | 1 | 62 → 96.9% | 62 → 92.5% |
| 8 | `findPaidAt` 시각 탐색 범위를 인접 줄 밖까지 넓히기 | 1 | 63 → 98.4% | 63 → 94.0% |
| — | **여기까지가 파서 규칙으로 닿는 한계** | **17** | **98.4%** | **94.0%** |
| — | 060: 정답표가 틀림(아래 각주) — 파서로 고칠 것 없음 | 1 | 규칙 밖 | 규칙 밖 |
| — | 026·027·030: 도달 불가 — 정답이 `visible_lines` 에 글자로 없음 | 3 | 분모 밖 | 규칙 밖 |

> **빠진 처방 하나 (각주).** 전 판에는 9번으로 **"`findCardNumber` 최소 자릿수를 12자 → 10자로
> 완화"** 가 있었다. **뺐다.** 근거였던 항목 `060` 의 실제 영수증에는 별표가 **16자**로 찍혀
> 있고 정답표가 10자로 잘못 적혀 있었다. 자릿수 문턱을 내려도 이 항목은 안 맞고 다른 영수증의
> 오탐만 는다. 고칠 곳은 파서가 아니라 정답표(카탈로그 v2)다.
>
> 이 판에서 규칙으로 닿는 한계가 **처방 9개·19건 65/67(97.0%) → 처방 8개·17건 63/67(94.0%)**
> 로 내려갔는데, 두 가지가 겹친 결과다. **9번 처방을 뺀 것이 1건(−1.5%p)**, `030` 을 도달
> 불가로 옮긴 것이 또 **1건(−1.5%p)** 이다.

**결론: 8개를 전부 고쳐도 전체 기준 94.0%(63/67) 에서 멈춘다.** 도달 가능 기준으로는 98.4%
(63/64)이고, 마지막 1건이 정답표 오류인 060 이다. **규칙만으로 99% 는 이 표본에서 불가능하다.**

### 동결본 v1 전체 기준 개선 목록

**아직 못 만들었다.** §4-2 의 원인 묶음이 사라진 판본 135장(오답 107건)에서 나온 것이라
v1 의 오답 103건에 그대로 못 쓴다. 건수별 개선 목록은 **v1 기준으로 오답을 다시 분류한 뒤에**
쓴다. 지금 확실한 것은 상한뿐이다.

| 기준 | 현재 | 규칙 상한 | 남는 것 |
|---|---:|---:|---|
| 도달 가능 (288필드) | 194 → 67.4% | 288 → 100% | — |
| 전체 (297필드) | 194 → 65.3% | 288 → **97.0%** | 도달 불가 9건은 2단계 Gemini 몫 |

그리고 규칙으로 닿는다고 해서 다 고칠 수 있다는 뜻은 아니다. 로고만 있는 상호, 임대인/입점매장
구분, 티켓 겸용 판형처럼 **상한 안쪽에도 규칙으로는 현실적으로 어려운 것**이 많다. 규칙 개선만의
현실적 상한은 90% 대 초반으로 본다.

## 합성 입력의 한계

이 숫자는 **합성 좌표**로 낸 것이다. 채점기는 카탈로그의 `visible_lines`(사람이 눈으로 읽은
글자 줄)를 `linesToFields()` 로 격자 좌표에 얹어 CLOVA 응답 모양을 만든다. 줄 높이 26,
줄 간격 30, 고정폭 13px, 넓은 공백(2칸 이상)은 좌우 단으로 크게 벌린다. 따라서 **검증되는
것은 파서의 규칙**(라벨 매칭, 정규식, 금액 다수결, 좌우 단 분리)뿐이고, **실제 OCR 이 만드는
잡음은 하나도 재현하지 못한다** — 기울어진 사각형, 글자 오인식, `lineBreak` 묶음이 위→아래
순서가 아니게 뒤섞여 나오는 현상, 들쭉날쭉한 줄 간격은 전부 빠져 있다. 게다가 `visible_lines`
자체가 사람이 잘 읽어 정리한 결과라 OCR 의 첫 단계 실패는 애초에 표본에 없다. 줄 재구성의
진짜 기준은 실제 응답 픽스처 3장(receipt-1/2/3, `supabase/functions/ocr/clova-general.fixtures.ts`)
이고 이 합성 입력은 그 위에 얹는 보조 자다. **여기서 99% 가 나와도 실제 인식률이 99% 라는
뜻이 아니다. 규칙 회귀를 잡는 용도로만 써라.**

## 분모에 대하여

분모는 `truth` 가 null 이 아닌 필드만 센다. 마스킹·저해상도로 사람도 못 읽어 정답이 null 인
필드는 "파서가 못 읽은 것"과 섞이면 숫자가 거짓말을 하므로 분모에서 뺐다. 그래서 분모가
필드마다 다르다. v1 앞 38 중 5장(004, 028, 029, 076, 088)과 동결본 v1 중 22장은 네 필드가
모두 null 이라 채점에서 통째로 빠졌다.

여기에 더해 **도달 가능 분모**가 따로 있다. `truth` 는 있는데 `visible_lines` 에 글자로 없는
필드를 뺀 것으로, 규칙으로 닿을 수 있는 최대치를 재는 분모다. 판정 방법은 맨 위
"도달 가능 / 도달 불가" 절에 적었다.

`paidAt` 의 부분 정답은 정답으로 세지 않았다. 날짜는 맞고 시각만 `00:00:00` 인 경우다
(v1 앞 38 기준 1건, 동결본 v1 기준 5건).
